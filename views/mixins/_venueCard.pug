mixin venueCard(venue = {})
    a(href=`/venues/${venue.id}` class="venue animated slideInUp")
        .venue__hero
            img(src=h.categoryIcon(venue) alt=`${h.getCategoryType(venue)}-Icon`)
            p #{venue.name}
